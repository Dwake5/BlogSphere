<h1>Author's Page</h1>

<% if @user.img_url %>
<img src="<%= @user.img_url %>" height="200" width="200" alt="">
<% else %>
<img src="<%= (url_for(@user.avatar)) %>" height="200" width="200" alt="">
<% end %>
<h3>Name of Author: <%= @user.name %> </h3>

<h3>Author's Posts:</h3>

<% @user.posts.each do |p| %>
<ul> <%= link_to p.title, p %>, published on <%= p.created_at %> </ul>
<% end %>